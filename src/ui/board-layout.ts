/**
 * BoardLayout — manages the overall solitaire board layout.
 *
 * Positions tableau, foundation, stock, and waste piles.
 * Handles responsive sizing and CSS grid/flex layout.
 * Implementation deferred to Story 3.
 */
export class BoardLayout {}
