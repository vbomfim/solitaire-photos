/**
 * DragController — handles drag-and-drop interactions for card moves.
 *
 * Manages pointer events, drag ghosts, drop targets, and snap-back.
 * Implementation deferred to Story 4.
 */
export class DragController {}
